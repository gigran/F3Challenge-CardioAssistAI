# F3Challenge-CardioAssistAI
# Dependencias diretas da primeira versao didatica do projeto.
# Ambiente-alvo: Python 3.12.10

# API e interface
fastapi==0.141.1
pydantic==2.13.4
uvicorn[standard]==0.52.4
streamlit==1.62.0

# LangChain, LangGraph e provedores de modelos
langchain==1.3.17
langchain-core==1.6.2
langgraph==1.2.11
langchain-text-splitters==1.1.2
langchain-openai==1.6.0
langchain-ollama==1.1.0
langsmith==0.11.1

# PostgreSQL e RAG com vetores
langchain-postgres==0.0.17
SQLAlchemy==2.0.52
psycopg[binary]==3.3.5
pypdf==5.1.0

# Modelo LoRA (fine-tuning e inferencia)
# torch com suporte a CUDA: instalar separadamente com o indice oficial, ex.:
#   python -m pip install torch==2.11.0+cu128 torchvision==0.26.0+cu128 --index-url https://download.pytorch.org/whl/cu128
# (nao usar este requirements.txt para o torch, pois o PyPI so publica build CPU)
unsloth
peft

# Configuracao por variaveis de ambiente
python-dotenv==1.2.3

# Testes e qualidade de codigo
httpx==0.28.1
pytest==9.1.1
ruff==0.16.4